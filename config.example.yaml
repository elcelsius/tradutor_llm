# Copie para config.yaml e ajuste conforme necessidade.
# Valores padrao mais seguros para chunk e timeout.

translate_chunk_chars: 2400   # tamanho maximo do chunk de traducao
refine_chunk_chars: 2400      # tamanho maximo do chunk de refine
request_timeout: 90           # timeout HTTP (segundos) nas chamadas de LLM

# Outros campos opcionais (descomente e ajuste se quiser):
# translate_backend: ollama
# translate_model: brunoconterato/Gemma-3-Gaia-PT-BR-4b-it:f16
# refine_backend: ollama
# refine_model: cnmoro/gemma3-gaia-ptbr-4b:q4_k_m
